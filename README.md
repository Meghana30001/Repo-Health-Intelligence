# Repo Health Intelligence

Real-time GitHub repository health analysis — frontend UI with a Node.js/Express backend.

## Setup

```bash
cd backend
npm install
npm start
```

Open `index.html` in a browser (or serve the project root with any static file server).

## Stack

- **Frontend:** HTML, CSS, JavaScript
- **Backend:** Node.js, Express, WebSocket, simple-git
